<%= form_for Project.new do |f| %>
  <% if Project.new.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(Project.new.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% Project.new.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit "Create Project"%>
  </div>
<% end %>
